query GetForm($formId: ID!) {
    form(id: $formId) {
        title
        id
        description
        questions {
            title
            type
            isRequired
            data {
                value
            }
        }
    }
}
